{% extends 'base.html.twig' %}


{% block body %}
<h3>Użytkownicy</h3>
<a href="{{ path("user_create") }}">Dodaj</a>

<ul>
    {% for user in users %}
        <li>
            <a href="{{ path('user_show', {'userId': user.id}) }}">{{ user.username }}</a>
        </li>
    {% endfor %}
</ul>
{% endblock %}