{% extends 'base.html.twig' %}


{% block body %}
<h3>Użytkownicy</h3>

<ul>
    {% for user in users %}
        <li>
            <a href="{{ path('user_show', {'userId': user.id}) }}">{{ user.username }}</a>
        </li>
    {% endfor %}
</ul>

{% if is_granted('ROLE_SUPER_ADMIN') %}
<a class="btn btn-primary" href="{{ path("user_create") }}">Dodaj</a>
{% endif %}

{% endblock %}